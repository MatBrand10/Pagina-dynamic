<div class="container sobre">
  <h1>Sobre: {{appName}}</h1>

  <p class="descricao">{{descricao}}</p>

  <h2>O que este projeto faz</h2>
  {{#if features}}
    <ul class="features-lista">
      {{#each features}}
        <li class="feature-item">• {{this}}</li>
      {{/each}}
    </ul>
  {{else}}
    <p>Sem funcionalidades listadas.</p>
  {{/if}}

  {{#if mostrarContato}}
    <div class="contato">
      <p>Quer contribuir ou tirar dúvidas? Clique no botão abaixo:</p>
      <button id="contactBtn" class="primary">Entrar em contato</button>
    </div>
  {{else}}
    <p>Contato indisponível no momento.</p>
  {{/if}}

  <p style="margin-top:18px;"><a href="/">← Voltar ao catálogo</a></p>
</div>